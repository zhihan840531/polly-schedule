// Polly 工作台：班級流程、考試提醒、簿本複查 v3
(function(){
  const STORE='polly_class_workflow_v1', PREP='polly_work_prep_v2';
  let data=load();
  const esc=s=>String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/"/g,'&quot;');
  const uid=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,7);
  function load(){try{return JSON.parse(localStorage.getItem(STORE)||'null')||{extraClasses:[],events:[],reviews:[]}}catch(e){return {extraClasses:[],events:[],reviews:[]}}}
  function save(){localStorage.setItem(STORE,JSON.stringify(data));window.dispatchEvent(new Event('polly-data-changed'));}
  function persistMain(){try{if(typeof persist==='function')persist();else if(typeof saveState==='function')saveState();}catch(e){}window.dispatchEvent(new Event('polly-data-changed'));}
  function baseClasses(){try{return typeof state!=='undefined'&&Array.isArray(state.classes)?state.classes:[]}catch(e){return []}}
  function classes(){const all=[...baseClasses(),...(data.extraClasses||[])],seen=new Set();return all.filter(c=>{const k=key(c);if(!k||seen.has(k))return false;seen.add(k);return true})}
  function key(c){return String(c.code||c.id||c.name||'').trim()}
  function label(c){return [c.name,c.code&&c.code!==c.name?c.code:''].filter(Boolean).join(' · ')||'未命名班級'}
  function typeOf(c){const s=String(c.code||c.name||'').toUpperCase();if(/^PH/.test(s))return '幼兒園部';if(c.workflowType)return c.workflowType;return '國小部'}
  function isPhonics(c){return /自然發音|發音|phonics?/i.test([c.name,c.code,c.title,c.courseName].filter(Boolean).join(' '))}
  function examEligible(c){return typeOf(c)==='國小部'&&!isPhonics(c)}
  function reviewEligible(c){return typeOf(c)!=='幼兒園部'&&!/^PH/i.test(String(c.code||c.name||''))}
  function roster(c){const d=Array.isArray(c.studentDetails)&&c.studentDetails.length?c.studentDetails:[];if(d.length)return d.map((s,i)=>({id:s.id||s.english||s.name||i,name:s.english||s.name||s.chinese||`學生 ${i+1}`}));return (c.students||[]).map((s,i)=>({id:i,name:typeof s==='string'?s:(s.english||s.name||s.chinese||`學生 ${i+1}`)}))}
  function closeModal(){document.getElementById('wfModal')?.remove()}
  function shell(title,body){closeModal();const m=document.createElement('div');m.id='wfModal';m.style.cssText='position:fixed;inset:0;z-index:9999;background:#0005;display:flex;align-items:center;justify-content:center;padding:18px';m.innerHTML=`<div style="width:min(92vw,440px);background:var(--yellow,#FFF2CC);border:1px solid #eadcae;border-radius:22px;padding:18px;box-shadow:0 18px 50px #0003"><h3 style="margin:0 0 14px">${esc(title)}</h3>${body}</div>`;document.body.appendChild(m);m.onclick=e=>{if(e.target===m)closeModal()};return m}
  function addClass(defaultType='國小部'){
    const fixed=defaultType==='冬夏令營';
    const m=shell('新增班級',`<label style="display:block;font-weight:800;margin:8px 0 6px">班級代碼</label><input id="wfClassCode" placeholder="例如 3A、PHB、2027冬令營A班" style="width:100%;box-sizing:border-box;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff"><label style="display:block;font-weight:800;margin:12px 0 6px">班級名稱</label><input id="wfClassName" placeholder="例如 3A、冬令營全天班" style="width:100%;box-sizing:border-box;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff"><label style="display:block;font-weight:800;margin:12px 0 6px">班級類型</label><select id="wfClassType" ${fixed?'disabled':''} style="width:100%;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff"><option ${defaultType==='國小部'?'selected':''}>國小部</option><option ${defaultType==='幼兒園部'?'selected':''}>幼兒園部</option><option ${defaultType==='冬夏令營'?'selected':''}>冬夏令營</option></select><div style="display:flex;gap:8px;margin-top:18px"><button class="btn" id="wfCancel" style="flex:1">取消</button><button class="btn primary" id="wfSave" style="flex:1">新增</button></div>`);
    m.querySelector('#wfCancel').onclick=closeModal;m.querySelector('#wfSave').onclick=()=>{const code=m.querySelector('#wfClassCode').value.trim(),name=m.querySelector('#wfClassName').value.trim()||code,type=m.querySelector('#wfClassType').value;if(!code)return alert('請輸入班級代碼');const c={id:Date.now(),code,name,workflowType:type,students:[],studentDetails:[],status:'on going'};try{state.classes=Array.isArray(state.classes)?state.classes:[];state.classes.unshift(c);persistMain();}catch(e){data.extraClasses.unshift({...c,custom:true});save()}closeModal();render();setTimeout(()=>{try{if(typeof renderClasses==='function')renderClasses()}catch(e){}},50)};
  }
  function addEvent(fixedClass){const cs=(fixedClass?[fixedClass]:classes().filter(examEligible)).filter(examEligible);if(!cs.length)return alert('目前沒有需要設定考試的班級');const m=shell('新增考試',`<label style="display:block;font-weight:800;margin:8px 0 6px">班級</label><select id="wfExamClass" style="width:100%;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff">${cs.map(c=>`<option value="${esc(key(c))}">${esc(label(c))}</option>`).join('')}</select><label style="display:block;font-weight:800;margin:12px 0 6px">考試類型</label><select id="wfExamType" style="width:100%;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff"><option>單字小考</option><option>單元考</option></select><label style="display:block;font-weight:800;margin:12px 0 6px">考試日期</label><input id="wfExamDate" type="date" style="width:100%;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff"><div style="display:flex;gap:8px;margin-top:18px"><button class="btn" id="wfCancel" style="flex:1">取消</button><button class="btn primary" id="wfSave" style="flex:1">儲存</button></div>`);m.querySelector('#wfCancel').onclick=closeModal;m.querySelector('#wfSave').onclick=()=>{const ck=m.querySelector('#wfExamClass').value,date=m.querySelector('#wfExamDate').value,type=m.querySelector('#wfExamType').value,c=cs.find(x=>key(x)===ck);if(!date)return alert('請先選擇考試日期');data.events.unshift({id:uid(),classKey:key(c),className:label(c),type,date,done:false});save();closeModal();render()}}
  function addReview(fixedClass){const cs=(fixedClass?[fixedClass]:classes().filter(reviewEligible)).filter(reviewEligible);if(!cs.length)return alert('目前沒有需要簿本複查的班級');const m=shell('新增簿本複查',`<label style="display:block;font-weight:800;margin:8px 0 6px">班級</label><select id="wfReviewClass" style="width:100%;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff">${cs.map(c=>`<option value="${esc(key(c))}">${esc(label(c))}</option>`).join('')}</select><label style="display:block;font-weight:800;margin:12px 0 6px">複查日期</label><input id="wfReviewDate" type="date" value="${new Date().toISOString().slice(0,10)}" style="width:100%;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff"><div style="display:flex;gap:8px;margin-top:18px"><button class="btn" id="wfCancel" style="flex:1">取消</button><button class="btn primary" id="wfSave" style="flex:1">建立</button></div>`);m.querySelector('#wfCancel').onclick=closeModal;m.querySelector('#wfSave').onclick=()=>{const c=cs.find(x=>key(x)===m.querySelector('#wfReviewClass').value),date=m.querySelector('#wfReviewDate').value;if(!date)return alert('請先選擇日期');data.reviews.unshift({id:uid(),classKey:key(c),className:label(c),date,students:roster(c).map(s=>({...s,first:'pending',second:'na'}))});save();closeModal();render()}}
  function daysUntil(s){const a=new Date();a.setHours(0,0,0,0);const b=new Date(s+'T00:00:00');return Math.ceil((b-a)/86400000)}
  function renderHome(){const page=document.getElementById('today');if(!page)return;let box=document.getElementById('workflowAlerts');if(!box){box=document.createElement('div');box.id='workflowAlerts';box.className='card';box.style.marginBottom='14px';page.insertBefore(box,page.querySelector('.grid')||page.firstChild)}const due=data.events.filter(e=>!e.done&&daysUntil(e.date)<=2),rev=data.reviews.filter(r=>r.students.some(s=>s.first==='pending'||s.second==='pending'));if(!due.length&&!rev.length){box.style.display='none';return}box.style.display='block';box.innerHTML='<h2 style="margin-top:0">🔔 近期提醒</h2>'+due.map(e=>`<div style="padding:8px 0;border-bottom:1px solid var(--line)"><b>📝 ${esc(e.className)}｜${esc(e.type)}</b><div style="font-size:12px;color:var(--muted)">${esc(e.date)} · ${daysUntil(e.date)<0?'已逾期':daysUntil(e.date)===0?'今天':daysUntil(e.date)+' 天後'}</div><button class="btn wfDone" data-id="${e.id}" style="margin-top:5px">已完成</button></div>`).join('')+rev.map(r=>`<div style="padding:8px 0"><b>📚 ${esc(r.className)}｜簿本複查</b><div style="font-size:12px;color:var(--muted)">初查未完成 ${r.students.filter(s=>s.first==='pending').length} 人｜待二次複查 ${r.students.filter(s=>s.second==='pending').length} 人</div></div>`).join('');box.querySelectorAll('.wfDone').forEach(b=>b.onclick=()=>{const e=data.events.find(x=>x.id===b.dataset.id);if(e)e.done=true;save();render()})}
  function reviewCard(r){const d=document.createElement('div');d.style.cssText='border:1px solid var(--line);border-radius:14px;padding:10px;margin:8px 0;background:#fffdf8';d.innerHTML=`<div style="display:flex;gap:8px;align-items:center"><b style="flex:1">${esc(r.className)}｜${esc(r.date)}</b><button class="btn danger del">刪除本輪</button></div><div class="rows"></div>`;const rows=d.querySelector('.rows');if(!r.students.length)rows.innerHTML='<div class="empty">此班目前沒有學生名單</div>';r.students.forEach(s=>{const row=document.createElement('div');row.style.cssText='display:grid;grid-template-columns:1fr auto auto;gap:6px;align-items:center;padding:7px 0;border-top:1px solid var(--line)';row.innerHTML=`<span>${esc(s.name)}</span><select class="first"><option value="pending">未檢查</option><option value="ok">初查完成</option><option value="needs">需二次複查</option></select><select class="second" ${s.first==='needs'?'':'disabled'}><option value="na">—</option><option value="pending">待複查</option><option value="ok">複查完成</option></select>`;row.querySelector('.first').value=s.first;row.querySelector('.second').value=s.second;row.querySelector('.first').onchange=e=>{s.first=e.target.value;s.second=s.first==='needs'?'pending':'na';save();render()};row.querySelector('.second').onchange=e=>{s.second=e.target.value;save();render()};rows.appendChild(row)});d.querySelector('.del').onclick=()=>{if(confirm('確定刪除這一輪簿本複查嗎？')){data.reviews=data.reviews.filter(x=>x.id!==r.id);save();render()}};return d}
  function renderWork(){const host=document.getElementById('pollyWorkPrep');if(!host)return;let panel=document.getElementById('classWorkflowPanel');if(!panel){panel=document.createElement('div');panel.id='classWorkflowPanel';host.appendChild(panel)}panel.innerHTML=`<div class="card" style="margin-top:14px"><div class="section-title"><h2>📅 開課中</h2><button class="btn" id="wfAddEvent">＋ 新增考試</button></div><div id="wfEvents"></div><div class="section-title" style="margin-top:16px"><h3 style="margin:0">📚 簿本複查</h3><button class="btn" id="wfAddReview">＋ 新增一輪</button></div><div id="wfReviews"></div></div>`;panel.querySelector('#wfAddEvent').onclick=()=>addEvent();panel.querySelector('#wfAddReview').onclick=()=>addReview();const ev=panel.querySelector('#wfEvents');data.events.forEach(e=>{const row=document.createElement('div');row.style.cssText='display:flex;gap:8px;align-items:center;padding:7px 0;border-bottom:1px solid var(--line)';row.innerHTML=`<input type="checkbox" ${e.done?'checked':''}><span style="flex:1"><b>${esc(e.className)}｜${esc(e.type)}</b><small style="display:block;color:var(--muted)">${esc(e.date)}（考前 2 天顯示首頁）</small></span><button class="btn danger">刪除</button>`;row.querySelector('input').onchange=x=>{e.done=x.target.checked;save();render()};row.querySelector('button').onclick=()=>{data.events=data.events.filter(x=>x.id!==e.id);save();render()};ev.appendChild(row)});const rv=panel.querySelector('#wfReviews');data.reviews.forEach(r=>rv.appendChild(reviewCard(r)))}
  function prepFor(c){let p;try{p=JSON.parse(localStorage.getItem(PREP)||'null')||{prep:{school:{},camp:{}}}}catch(e){p={prep:{school:{},camp:{}}}}const sec=typeOf(c)==='冬夏令營'?'camp':'school';return {p,sec,rec:p.prep?.[sec]?.[key(c)]}}
  function savePrep(p){localStorage.setItem(PREP,JSON.stringify(p));window.dispatchEvent(new Event('polly-data-changed'))}
  function renderClassWork(){const box=document.getElementById('detailClassWork');if(!box)return;let id=null;try{id=currentDetailClassId}catch(e){}const c=baseClasses().find(x=>String(x.id)===String(id));if(!c){box.innerHTML='';return}const {p,sec}=prepFor(c);p.prep=p.prep||{school:{},camp:{}};p.prep[sec]=p.prep[sec]||{};p.prep[sec][key(c)]=p.prep[sec][key(c)]||{items:[],note:''};const rec=p.prep[sec][key(c)],events=data.events.filter(e=>e.classKey===key(c)),reviews=data.reviews.filter(r=>r.classKey===key(c));box.innerHTML=`<div class="s360-card" style="margin-top:10px"><div class="section-title"><h3 style="margin:0">🎒 開課準備</h3><span class="badge">${rec.items.filter(x=>x.done).length}/${rec.items.length}</span></div><div id="cwPrep"></div></div>${examEligible(c)?`<div class="s360-card" style="margin-top:10px"><div class="section-title"><h3 style="margin:0">📝 考試</h3><button class="btn" id="cwAddExam">＋ 新增</button></div><div>${events.length?events.map(e=>`<div style="padding:8px 0;border-top:1px solid var(--line)">${esc(e.type)}｜${esc(e.date)} ${e.done?'✓':''}</div>`).join(''):'<div class="empty">尚未建立考試</div>'}</div></div>`:''}${reviewEligible(c)?`<div class="s360-card" style="margin-top:10px"><div class="section-title"><h3 style="margin:0">📚 簿本複查</h3><button class="btn" id="cwAddReview">＋ 新增</button></div><div>${reviews.length?reviews.map(r=>`<div style="padding:8px 0;border-top:1px solid var(--line)">${esc(r.date)}｜初查未完成 ${r.students.filter(s=>s.first==='pending').length} 人｜待複查 ${r.students.filter(s=>s.second==='pending').length} 人</div>`).join(''):'<div class="empty">尚未建立複查</div>'}</div></div>`:''}`;const list=box.querySelector('#cwPrep');if(!rec.items.length)list.innerHTML='<div class="empty">尚未建立確認事項</div>';rec.items.forEach(it=>{const row=document.createElement('label');row.style.cssText='display:flex;gap:9px;align-items:center;padding:8px 0;border-top:1px solid var(--line)';row.innerHTML=`<input type="checkbox" ${it.done?'checked':''} style="width:20px;height:20px"><span>${esc(it.title)}</span>`;row.querySelector('input').onchange=e=>{it.done=e.target.checked;savePrep(p);renderClassWork()};list.appendChild(row)});box.querySelector('#cwAddExam')?.addEventListener('click',()=>addEvent(c));box.querySelector('#cwAddReview')?.addEventListener('click',()=>addReview(c))}
  function enhanceClassDetail(){const tabs=document.querySelector('.detail-tabs'),lessons=document.getElementById('detailLessons');if(!tabs||!lessons)return;if(!document.getElementById('tabClassWork')){const b=document.createElement('button');b.id='tabClassWork';b.textContent='班級工作';b.onclick=()=>window.switchClassTab('work');tabs.appendChild(b);const div=document.createElement('div');div.id='detailClassWork';div.style.display='none';lessons.insertAdjacentElement('afterend',div)}if(!window.__pollyWrappedTabs&&typeof window.switchClassTab==='function'){const old=window.switchClassTab;window.switchClassTab=function(tab){const work=tab==='work';if(work){document.getElementById('detailStudents').style.display='none';document.getElementById('detailLessons').style.display='none';document.getElementById('detailClassWork').style.display='block';document.getElementById('tabStudents').classList.remove('active');document.getElementById('tabLessons').classList.remove('active');document.getElementById('tabClassWork').classList.add('active');renderClassWork();return}old(tab);document.getElementById('detailClassWork').style.display='none';document.getElementById('tabClassWork').classList.remove('active')};window.__pollyWrappedTabs=true}}
  function migrateExtras(){try{if(!data.extraClasses?.length||typeof state==='undefined')return;state.classes=Array.isArray(state.classes)?state.classes:[];let changed=false;data.extraClasses.forEach(c=>{if(!state.classes.some(x=>key(x)===key(c))){state.classes.unshift(c);changed=true}});if(changed)persistMain();data.extraClasses=[];save()}catch(e){}}
  window.openWorkflowAddClass=addClass;
  function render(){renderWork();renderHome();enhanceClassDetail();if(document.getElementById('detailClassWork')?.style.display!=='none')renderClassWork()}
  function init(){migrateExtras();render();setTimeout(render,900);setInterval(render,2500)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();